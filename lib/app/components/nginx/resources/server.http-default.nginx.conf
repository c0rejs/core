server {
<% if ( localAddress ) { -%>
    listen                  "<%- localAddress %>"<%- listenOptions %>;
<% } else { -%>
<% if ( nginx.listenIpV4 ) { -%>
    listen                  *:<%- server.port %><%- server.getListenOptions() %>;
<% if ( server.useQuic ) { -%>
    listen                  *:<%- server.port %><%- server.getListenOptionsQuic() %>;
<% } -%>
<% } -%>
<% if ( nginx.listenIpV6 ) { -%>
    listen                  [::]:<%- server.port %><%- server.getListenOptions() %>;
<% if ( server.useQuic ) { -%>
    listen                  [::]:<%- server.port %><%- server.getListenOptionsQuic() %>;
<% } -%>
<% } -%>
<% } -%>
<% if ( server.useSsl ) { -%>

    ssl_reject_handshake    on;
<% } else { -%>

    real_ip_header          <%- server.useProxyProtocol ? "proxy_protocol" : nginx.config.realIpHeader %>;

    location / {
        return              444;
    }
<% if ( server.acmeLocation ) { -%>

    location <%- nginx.app.acme.httpLocation %> {
        proxy_pass          "http://<%- nginx.privateHrrpServerUpstream %>";
    }
<% } -%>
<% } -%>
}
