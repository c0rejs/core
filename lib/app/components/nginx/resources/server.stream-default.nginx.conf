server {
<% if ( localAddress ) { -%>
    listen                  "<%- localAddress %>"<%- server.listenOptions %>;
<% } else { -%>
<% if ( nginx.listenIpV4 ) { -%>
    listen                  *:<%- server.port %><%- server.listenOptions %>;
<% } -%>
<% if ( nginx.listenIpV6 ) { -%>
    listen                  [::]:<%- server.port %><%- server.listenOptions %>;
<% } -%>
<% } -%>

    ssl_reject_handshake    on;
}
